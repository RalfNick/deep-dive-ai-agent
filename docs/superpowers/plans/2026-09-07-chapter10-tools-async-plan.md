# 第十章执行计划

设计见同日期 specs 文件；当前任务内执行，不另开任务。

- [x] 写目录与作业边界的失败测试，运行并确认缺实现导致失败。
- [x] 实现目录、有限并发、SQLite 作业，验证权限、版本、重复提交、取消和恢复。
- [x] 实现五组实验与确定性证据报告，二次生成比对。
- [x] 写完整正文、14 道练习和答案、来源台账。
- [x] 制作六张原创图，检查中文、箭头和状态语义；生成本地预览。
- [x] 读者与技术自审，修复发现的问题并补测试。
- [x] 运行章节/仓库回归、链接与敏感信息检查，记录候选版本。
- [x] 保存提交，将候选分支同步至 D 盘工程；不自动发布。

完成记录：正文提交 `b11240b` 已同步到 D 盘独立书库的同名候选分支，并在目标工程生成本地 HTML 预览。main 仍保持 `02b71aa`，既有发布 tag 不变；本轮没有 GitHub 推送或网站部署。37 项章节检查、43 项仓库检查、443 项已发布章节回归、4 项 Node 排版检查与既有站点 strict 构建通过。

验证命令预定：`python -m unittest discover -s chapter10/tests -v`，`python -m chapter10.experiments --write`，`python -m unittest discover -s tests -v`。所有故障用固定输入和逻辑时间；实际协程测试以屏障和峰值在途数证明并发，不用机器耗时猜测是否并发。
