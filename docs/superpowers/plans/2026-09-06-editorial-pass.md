# 第 1–9 章读者路径优化 Implementation Plan

> **For agentic workers:** 按用户已确认的本轮 Review 在当前任务逐项执行，不另建任务；以独立编辑副本隔离修改，逐项验证后同步回书籍工程。

**Goal:** 修正图文与交付不一致，降低阅读负担，保留已有工程深度及历史版本。

**Architecture:** 权威入口仍是 `book/chapterN.md`。正文保留核心机制与最小实验；完整审核清单和重复 FAQ 移到可链接的进阶材料，不改实验运行时的既有语义。

**Tech Stack:** Markdown、Python 标准库、现有实验包、原生图片编辑、MkDocs。

**Spec:** 用户确认的六项 Review：图中 Softmax 对应、真实 Embedding 交付表述、float 合同、第五六章先例子后对象、全书删重、计算题完整答案。

## Global Constraints

- 不翻译、不改写旧 tag、不覆盖旧图片、不执行发布。
- 编辑基线为 `46be2bbca32dde80abadff65f20884407441859d`；记录修订范围和本地候选状态。
- 不调用付费模型实验；真实模型能力不得由确定性实验推断。
- 保留原有实验、引用、图和练习入口；正文长度以现有写作标准 1.8–3 万中文字符为指导，不以重复论述满足硬下限。

## Tasks

- [x] 保存版本基线及修订记录；运行现有仓库检查作为基线。
- [x] 第一章以版本化新图替换入口，候选采用 sampling_demo.py 的 2.0/1.0/0.1 与 T=1；人工校图并运行采样核对。第二章统一 float 验收，不改变已核对产品事实。
- [x] 第五章插入四行小输入的装配例，后移完整实验合同；明确仓库/用户优先级是教学策略。第六章先展示一句话交接与四项任务卡，后移七类状态表和 Artifact 字段审计细节。
- [x] 第八章如实说明 Live Probe 范围、缩短练习后总结、补完整 BM25/RRF 算例；增加打印计算结果的标准库入口及失败先行的命令验证测试。第九章 FAQ 移到独立补充材料、保留关键入口，下一章名与总目录一致。
- [x] 同步版本表、阅读指南、来源和 Review；检查章节合同、安全扫描、数学结果、Markdown 链接与严格站点构建。确认旧图和基线仍存在。本地候选已准备好；同步到原工程保留独立分支，不合并主分支、不推送。

## Verification

先运行第八章新增 CLI 测试，确认缺少入口时失败；实现后用手算结果验证排序与分数。正文以人工对照和已有发布检查验证，不编写绑定特定措辞的测试。若精简使第八九章低于旧 2.5 万字门槛，将其统一到全书 1.8 万字指导下限，保留图、题、来源、安全等所有其他检查，并记录缘由。
