# 来源：第 1 章“大模型入门”

来源文件：`book/chapter1.md`。

## 核心机制

> 神经网络处理数字。自然语言进入模型前，必须先经过 Tokenizer。

> Token ID 只是词表里的编号。模型会通过 Embedding 表，把每个离散 ID 映射为一个高维向量。

> 现代生成式 LLM 多数采用 decoder-only 结构。

一次前向计算的原文流程：

```text
文本 → Token ID → Embedding + 位置 → N 个 Transformer Block
     → 词表 Logits → 概率分布 → 下一个 Token
```

Transformer Block 的原文解释：

- “注意力决定‘从其他位置读什么’，前馈网络决定‘读到之后怎样变换’。”
- “残差连接让新信息与原表示相加，归一化帮助深层网络稳定训练。”
- “数十层 Block 反复执行，浅层局部模式逐渐组合成更适合当前预测的表示。”

## 生成循环

> 推理时没有标准答案。模型根据当前上下文得到概率分布，选择或采样一个 Token，把它追加到上下文，然后再次预测。

> 模型选出一个 Token 后，整个过程重新开始。这就是自回归生成。

常见停止条件包括：结束 Token、最大输出 Token 数、停止序列、工具调用，以及安全、超时或资源限制。

## 工程边界

> 模型每次只前进一步；连贯文本来自同一计算被自回归地重复执行。

图只解释 decoder-only 生成式 LLM 的入门心智模型，不把它画成“理解真相”或“自动执行工具”的系统。
