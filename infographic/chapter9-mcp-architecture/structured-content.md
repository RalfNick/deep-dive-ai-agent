# MCP：Host、Client、Server

- 用户 → Host。
- Host：上下文、安全、同意、多连接管理。
- Client A → Server A；Client B → Server B；一对一连接。
- Server：Tools、Resources、Prompts；业务授权。
- Server 之间不共享完整对话或彼此数据。

底部结论：Host 管安全与上下文，Client 管连接，Server 管能力。
