# 第 10 章 v1.1 修订与复核

日期：2026-09-08。状态：本地修订稿，尚未发布。基线：`book-chapter10-v1.0`（`c50a5ec`）。这是本轮修改后的自查，不是独立外部审计。

## 发现与处置

| Review 发现 | 修订 | 验证 |
| --- | --- | --- |
| 截止时间先于幂等查询检查，导致已成功作业无法通过同键重试找回 | 身份与意图检查后，在同一事务中先查既有意图；仅对新作业执行期限、容量、尝试上限准入检查 | 修改前重现成功/失败/取消三种终态恢复失败和过期后的冲突错误；修改后通过；新意图仍拒绝过期期限，原预算不被重试覆盖 |
| “经过验证的结果”混淆运行控制与业务正确性 | 正文与 `finish()` 文档明确两者职责；用空字典也能提交的反例解释范围 | 通用作业库不新增报表专属验证；现有固定数据测试及新入口测试独立断言月份、3 条记录与 6,000 分 |
| 后半章接口多，读者难以连接顺序 | 增加完整可运行的 quickstart 和正文核心流程，先正常路径后故障 | 真实 subprocess 模块命令测试覆盖三种可见状态、三次进度、唯一回执与报表结果 |
| 图 10-4 省略分支但没有在读者可见处注明 | 新版 PNG 在图内注明“常见状态路径 / 示意图，非完整状态图”，正文补状态表 | 目检图片文字与箭头；保留旧图；表格补充重试、排队取消、过期重领与终态保持 |
| 第 8 题答案序号错位 | 改为“参数值为 3 的调用（第四个调用，ID 为 3）” | 与 `range(5)` 的参考实现核对 |

## 本轮验证

- 第 10 章 42 项测试通过；新增 4 个幂等/准入回归测试方法和 1 个 quickstart 命令测试。
- 仓库合同 43 项、Node 渲染合同 4 项通过。最初仓库检查发现工作副本旧 Python 缓存，已移到工程外的可恢复备份，再次运行通过；没有削弱检查。
- 原五组实验报告在临时目录重新生成，与版本库三份报告逐字节一致。新反例由专项测试记录，不修改旧报告的测量口径。
- 本地 HTML 已生成；既有浏览器检查在 1280px 和 390px 下通过，6 图加载成功，6 个图注、10 条脚注，失效片段链接 0，整页横向溢出 0。
- 网站源文件提取与 MkDocs strict 构建成功；这只是本地构建，不代表部署。

## 阅读判断与保留边界

保留订单工具发现与报表作业两个局部案例。新增流程使读者先看到完整调用链，再理解失败分支；无需先搭建队列、服务器或真实模型。

仍未加入真实模型、外部工具适配器、后台调度守护进程、续租、业务运行时验收器和分布式副作用保证。quickstart 使用有限固定夹具与逻辑时间；测试算对一份报表不代表生产数据与所有业务规则均被验证。v1.0 的自审文件保留，本文件补充本次新发现与修订。
