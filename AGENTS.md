# AGENTS.md

本文件约束在《深入浅出 AI Agent》独立仓库内工作的自动化 Agent。

## 权威范围

- `book/` 是简体中文唯一权威正文。
- `book-en/` 当前仅允许 `status: planned` 的说明文件；未批准前不要生成译文章节。
- 不创建繁体中文目录。
- `chapter1/`–`chapter6/` 是与正文对应的实验包；每个包必须保持独立可运行。

## 写作与证据

- 先讲概念边界，再讲架构、代码、权衡和失败案例。
- 核心结论应映射到本仓库代码、固定报告或来源台账。
- 产品事实使用官方来源，明确核对日期和可能变化的边界。
- 不把离线字节数叫作 Token，不把单案例叫作成功率，不做无证据厂商排名。
- 修改章节时同步检查图片、练习、参考答案、来源、Review 和下一章衔接。

## 工程与安全

- 实现或修复先写失败测试；完成前运行与风险匹配的验证。
- 不提交 Secret、`.env`、缓存、live reports、PDF/HTML 构建物或作者机器绝对路径。
- 规范报告必须可复现；生成器默认不得注入当前时间、随机 UUID 或临时路径。
- Git 暂存使用精确路径，不使用 `git add .`、`-A` 或 `--all`。
- 不重写公开历史，不移动既有 tag，不在未验证迁移前删除源工程内容。

## 章节状态

第 1–6 章已发布；第 7–18 章只有规划。不要把规划标题、预留 Pages URL、PDF 或英文版描述成已经交付。
